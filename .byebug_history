c
    self.referral_link = "app.onrop.pl/r/#{based_token}"
    based_token = "#{username}_#{SecureRandom.urlsafe_base64(6)}"
"#{username}_#{SecureRandom.urlsafe_base64(6)}"
    based_token = "#{username}#{SecureRandom.urlsafe_base64(6)}"
    username = "#{name.downcase.split[0]}"
self.referral_link = ""
email
id
referral_link
ap self
self
